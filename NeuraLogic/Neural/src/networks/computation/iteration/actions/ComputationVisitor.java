package networks.computation.iteration.actions;

import networks.computation.values.Value;
import networks.structure.neurons.Neurons;

import java.util.logging.Logger;

public class ComputationVisitor extends NeuronVisitor<Value> {
    private static final Logger LOG = Logger.getLogger(ComputationVisitor.class.getName());

}
